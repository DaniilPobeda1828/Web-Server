# Web-Server: Техническая документация проекта

## 📋 Обзор проекта
**Web-Server** — веб-серверное приложение на Java с использованием Spring Boot. Проект реализует RESTful API для управления пользователями и их активностью. **Статус**: активная разработка, 9 коммитов. **Репозиторий**: [https://github.com/DaniilPobeda1828/Web-Server](https://github.com/DaniilPobeda1828/Web-Server)

## 🏗️ Архитектура и структура
**Технологии**: Java 100%, Spring Boot, Maven Wrapper. **Структура**:
```
Web-Server/
├── .mvn/wrapper/           # Maven Wrapper
├── src/                    # Исходный код
├── tests/                  # Тесты (удалены)
├── .gitattributes
├── .gitignore
├── mvnw                   # Linux скрипт
├── mvnw.cmd               # Windows скрипт
└── pom.xml                # Конфигурация
```
**История коммитов**:
- 01.02.2026: Delete tests/test — удаление тестов
- 01.02.2026: Создание структуры проекта — инициализация
- 01.02.2026: Создание контроллера UserActivityController — добавление REST API

## 🔧 Установка и запуск
**Требования**: Java JDK 17+, Git.
```bash
git clone https://github.com/DaniilPobeda1828/Web-Server.git
cd Web-Server
./mvnw clean compile
./mvnw spring-boot:run
```
**Проверка**: После запуска сервер доступен по `http://localhost:8080`. В консоли: `Tomcat started on port(s): 8080`.

## 📁 Структура кода
```
src/main/java/com/your_package/app/
├── AppApplication.java               # Главный класс (@SpringBootApplication)
├── controller/                       # REST-контроллеры
│   └── UserActivityController.java   # Обработка запросов пользователей
├── service/                          # Бизнес-логика
├── repository/                       # Доступ к данным
└── model/                            # Доменные модели
    ├── User.java
    └── Post.java
```

## 🎯 Функциональность
**Основной компонент**: `UserActivityController` — обработка HTTP-запросов к пользователям.

### Предполагаемые REST эндпоинты:
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/users` | Все пользователи |
| GET | `/users/{id}` | Конкретный пользователь |
| POST | `/users` | Создать пользователя |
| PUT | `/users/{id}` | Обновить пользователя |
| DELETE | `/users/{id}` | Удалить пользователя |
| POST | `/users/{id}/posts` | Добавить пост |

### Модели данных:
```java
// User.java
public class User {
    private Long id;
    private String username;
    private String email;
    // геттеры/сеттеры
}

// Post.java
public class Post {
    private Long id;
    private String title;
    private String content;
    private Long userId;
    // геттеры/сеттеры
}
```

## ⚙️ Конфигурация
**application.properties**:
```properties
server.port=8080
spring.application.name=Web-Server
spring.devtools.restart.enabled=true
logging.level.com.your_package=DEBUG
```
**pom.xml зависимости**:
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
```

## 🧪 Тестирование
**API тестирование** (Postman/cURL):
```bash
curl -X GET http://localhost:8080/users
```
**Юнит-тесты**: Рекомендуется восстановить в `src/test/java/` с JUnit 5 и Mockito.

## 🔍 Распространенные проблемы
1. **Порт 8080 занят**: Изменить в `application.properties`: `server.port=8081`
2. **Ошибки запуска**: Проверить Java версию и зависимости Maven
3. **404 Not Found**: Убедиться, что контроллер имеет правильные аннотации `@RestController` и `@RequestMapping`

## 🚀 Дальнейшее развитие
1. **Восстановление тестов** — добавление юнит- и интеграционных тестов
2. **База данных** — интеграция с H2/PostgreSQL через Spring Data JPA
3. **Валидация** — проверка данных с `@Valid`
4. **Документация API** — подключение Swagger/OpenAPI
5. **Безопасность** — добавление Spring Security
6. **Контейнеризация** — создание Dockerfile

## 📞 Контакты
**Автор**: DaniilPobeda1828  
**Репозиторий**: [https://github.com/DaniilPobeda1828/Web-Server](https://github.com/DaniilPobeda1828/Web-Server)

## 📝 Заключение
**Web-Server** — типичное Spring Boot приложение, демонстрирующее базовые принципы REST API. Проект служит хорошей основой для изучения Spring Boot и может быть расширен для реального использования. Текущее состояние (структура + контроллер) идеально для дальнейшей разработки.
